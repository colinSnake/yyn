const config = {
    mysqlConfig: {
        host: 'localhost',
        port: '3306',
        user: 'root',
        password: '123456',
        database: 'yyn',
        socketPath: '/tmp/mysql.sock'
    }
}

module.exports = config;
